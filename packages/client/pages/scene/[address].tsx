import type { NextPage } from "next";
import { useRouter } from "next/router";

type Props = {};

const Address: NextPage<Props> = (props: Props) => {
  return <div>[address]</div>;
};

export default Address;
